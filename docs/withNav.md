# withNav

`withNav` is higher-order component. It will pass function `navigate` as prop to children.


#### navigate(to: string|number)

`to` as a `string` pushes url to history

`to` as a `number` navigates forward or backward 